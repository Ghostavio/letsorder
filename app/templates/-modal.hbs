<div class="overlay hidden" {{ action "close" }}>
  <div class="modal">
    <div class="close" {{ action "close" }}>X</div>
    <div {{ action bubbles=false }}>
      {{ outlet modalcontent }}
    </div>
  </div>
</div>
