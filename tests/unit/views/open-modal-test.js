import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:open-modal', 'OpenModalView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
